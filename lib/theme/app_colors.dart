import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromARGB(
    255,
    17,
    180,
    198,
  ); // Neon Cyan for premium accents
  static const Color textMain = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color background = Color(0xFF0B0F19); // Rich dark background
  static const Color lightBackground = Color(0xFF111827); // Surface color
  static const Color cardBorder = Color(0x3300E5FF); // Faint cyan border
}
